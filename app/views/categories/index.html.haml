- log_object_view '/categories/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', categories_path
    = link_to 'new', new_category_path, :class=>'new large'
  
= view_title 'Categories', :list

= render :partial=>'list'

= will_paginate @objects
