- log_object_view '/categories/new'

.top_actions.buttons
  .buttons_inner
    = RDisplay.cancel_form_link("/categories")
    = RDisplay.submit_form_link("new_category")

= view_title @object, :new

= form_for(@object) do |f|

  = render :partial=>'/shared/admin/form_errors'
  = render :partial=>'/shared/admin/form_details',    :locals=>{:f=>f}
          
.form_actions.buttons
  .buttons_inner
    = RDisplay.cancel_form_link("/categories")
    = RDisplay.submit_form_link("new_category")
    
