- log_object_view '/enrollments/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', enrollments_path
    = link_to 'new', new_enrollment_path, :class=>'new large'
  
= view_title 'Enrollment', :list

= render :partial=>'list'

= will_paginate @objects
