- log_object_view 'pages/edit_meta'

.top_actions.buttons
  .buttons_inner
    = RDisplay.cancel_form_link("/pages/#{@object.id}?show[meta]=yes")
    = RDisplay.submit_form_link("edit_page_#{@object.id}")

= view_title @object, :edit

= form_for(@object) do |f|

  = render :partial=>'/shared/admin/form_errors'
  = render :partial=>'/shared/admin/form_meta', :locals=>{:f=>f}
          
.form_actions.buttons
  .buttons_inner
    = RDisplay.cancel_form_link("/pages/#{@object.id}?show[meta]=yes")
    = RDisplay.submit_form_link("edit_page_#{@object.id}")
