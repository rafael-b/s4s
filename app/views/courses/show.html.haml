- log_object_view '/courses/show'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'list', courses_path, :class=>'list'
    = link_to 'new', new_course_path, :class=>'new'
    = buttons_divider
    - if @object.active
      = link_to 'disable', "/courses/#{@object.id}/activate", :class=>'disable'
    - else
      = link_to 'activate', "/courses/#{@object.id}/activate", :class=>'activate'
    = buttons_divider
    = link_to 'publish', "/courses/#{@object.id}/publish", :class=>'publish'
  

= render :partial=>'/shared/admin/show_object'

