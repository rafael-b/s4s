- log_object_view '/courses/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', courses_path
    = link_to 'new', new_course_path, :class=>'new large'
  
= view_title 'Courses', :list

= render :partial=>'list'

= will_paginate @objects
