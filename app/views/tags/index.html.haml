- log_object_view 'tags/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', tags_path
    = link_to 'new', new_tag_path, :class=>'new large'
  
= view_title 'Tags', :list

= render :partial=>'list'
= will_paginate @objects
