- log_object_view '/blogs/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', blogs_path
    = link_to 'new', new_blog_path, :class=>'new large'
  
= view_title 'Blogs', :list

= render :partial=>'list'

= will_paginate @objects
