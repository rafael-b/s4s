- log_object_view '/emails/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', emails_path
    -#= link_to 'new', new_email_path, :class=>'new large'
  
= view_title 'Emails', :list

= render :partial=>'list'

= will_paginate @objects
