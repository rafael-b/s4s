- log_object_view '/documents/new'

.top_actions.buttons
  .buttons_inner
    = RDisplay.cancel_form_link("/documents")
    = RDisplay.submit_form_link("new_document")

= view_title @object, :new

= form_for(@object) do |f|

  = render :partial=>'/shared/admin/form_errors'
  = render :partial=>'/shared/admin/form_details',  :locals=>{:f=>f,:required=>[:name]}
  
.form_actions.buttons
  .buttons_inner
    = RDisplay.cancel_form_link("/documents")
    = RDisplay.submit_form_link("new_document")
    
