- log_object_view '/documents/show'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'list', documents_path, :class=>'list'
    = link_to 'new', new_document_path, :class=>'new'
    = buttons_divider
    - if @object.active
      = link_to 'disable', "/documents/#{@object.id}/activate", :class=>'disable'
    - else
      = link_to 'activate', "/documents/#{@object.id}/activate", :class=>'activate'
    = link_to 'publish', "/documents/#{@object.id}/publish", :class=>'publish'
  

= render :partial=>'/shared/admin/show_object'

