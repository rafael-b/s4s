- log_object_view '/lectures/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', lectures_path
    = link_to 'new', new_lecture_path, :class=>'new large'
  
= view_title 'Lectures', :list

= render :partial=>'list'

= will_paginate @objects
