- log_object_view '/quizzes/show'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'list', quizzes_path, :class=>'list'
    = link_to 'new', new_quiz_path, :class=>'new'
    = buttons_divider
    - if @object.active
      = link_to 'disable', "/quizzes/#{@object.id}/activate", :class=>'disable'
    - else
      = link_to 'activate', "/quizzes/#{@object.id}/activate", :class=>'activate'
    = buttons_divider
    = link_to 'publish', "/quizzes/#{@object.id}/publish", :class=>'publish'
  

= render :partial=>'/shared/admin/show_object'

