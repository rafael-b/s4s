- log_object_view '/quizzes/index'

= render :partial=>'/layouts/admin/filters'

.top_actions.buttons
  .buttons_inner
    = link_to 'refresh', quizzes_path
    = link_to 'new', new_quiz_path, :class=>'new large'
  
= view_title 'Quizzes', :list

= render :partial=>'list'

= will_paginate @objects
